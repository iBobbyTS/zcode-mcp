{
  "name": "zcode-subagent-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "zcode-as-subagent MCP facade and fail-closed policy hooks.",
  "scripts": {
    "check": "node --check hooks/check-bash-readonly.mjs && node --check hooks/audit-bash-result.mjs && node --check lib/bash-policy.mjs",
    "test": "node --test test/*.test.mjs",
    "install": "node scripts/install-agent-hooks.mjs",
    "check:hooks": "node scripts/check-agent-hooks.mjs",
    "preflight": "node scripts/preflight-agent-hooks.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
