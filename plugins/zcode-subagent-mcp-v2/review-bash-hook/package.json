{
  "name": "zcode-readonly-bash-guard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed, path-confined read-only Bash hook for ZCode review sessions.",
  "scripts": {
    "build": "node scripts/build-single-file.mjs",
    "checksums": "node scripts/update-checksums.mjs",
    "check": "node --check hooks/check-bash-readonly.mjs && node --check hooks/audit-bash-result.mjs && node --check lib/readonly-bash-policy.mjs",
    "test": "node --test test/*.test.mjs",
    "install:hook": "node ../scripts/install-review-hook.mjs",
    "check:hook": "node ../scripts/check-review-hook.mjs",
    "preflight:hook": "node ../scripts/preflight-review-hook.mjs",
    "verify": "npm run build && npm run checksums && npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
