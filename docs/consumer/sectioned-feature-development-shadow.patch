--- a/SKILL.md
+++ b/SKILL.md
@@ -289,0 +290,7 @@
+
+### Optional ZCode GLM shadow evidence
+
+When a repository explicitly configures the `sectioned-zcode-shadow/v1`
+adapter, a GLM review may run beside the normal GPT reviewer as optional,
+non-authoritative evidence. Shadow rollout never becomes a required gate and
+does not replace the reviewer routing above.
@@ -290,0 +298,18 @@
+- A counted full plan, `INITIAL_BOUNDED`, or `FINAL_BOUNDED` shadow run must use
+  a newly created Agent and a fresh nonempty ZCode session with matching
+  manifest/prepared/prompt/base/head provenance. `existing_compatible`, resume,
+  or `REPAIR_DELTA` is consultation only and cannot count as fresh evidence.
+- Do not include prior GPT/GLM RAW, admission, conclusion, or session transcript
+  artifacts in counted GLM context.
+- Keep evidence in separate `*-GPT-RAW.md`, `*-GPT-ADMISSION.md`,
+  `*-GLM-RAW.md`, `*-GLM-PROVENANCE.json`, and `*-GLM-ADMISSION.md` artifacts.
+  These supplement the compact authoritative section review ledger; they do not
+  replace it.
+- The main Codex agent remains the sole admission owner and writes the GLM
+  admission projection. GLM cannot approve, accept, merge, change Clean A/Clean
+  B, change repair caps, trigger recovery, or change section sequencing.
+- Infrastructure/runtime failure, unsupported user input, missing fresh
+  provenance, or incomplete report evidence is `evidence_incomplete`, never a
+  clean result. A DELTA shadow run is targeted consultation and never adds an
+  independent clean count.
+
